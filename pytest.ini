[pytest]
# `testpaths` + a raiz no sys.path deixam `pytest` (sem argumento) rodar a
# suíte inteira de qualquer lugar do projeto, importando `src.*` sem instalar
# o pacote.
testpaths = tests
pythonpath = .
addopts = -q
